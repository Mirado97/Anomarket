import ProfileLayout from '../../components/ProfileLayout';
export default function ActivityPage() {
    return ( <ProfileLayout><p>This is where your activity will be shown.</p></ProfileLayout> );
}